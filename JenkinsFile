pipeline {
 agent any
 stages {
   stage("shiva") {
    steps {
          script {
           checkout scm
           sh 'rm -rf *.war'
           
           sh 'echo ${BUILD_TIMESTAMP}'
           }
    }
   }
  }
}
