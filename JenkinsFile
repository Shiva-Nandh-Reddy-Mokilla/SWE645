pipeline {
 agent any
 stages {
   stage("shiva") {
    steps {
          script {
           checkout scm
           sh 'rm -rf *.war'
           sh 'jar -cvf assignment.war -C WebContent/ .'
           sh 'echo ${BUILD_TIMESTAMP}'
           }
    }
   }
  }
}
